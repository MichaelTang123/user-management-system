spring:
  aop:
    proxyTargetClass: false
  application:
    name: restapi-service
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:log4jdbc:mysql://127.0.0.1:3306/testdb

  
    